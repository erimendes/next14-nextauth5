// auth.js docs
import { handlers } from "~/lib/auth";
export const { GET, POST } = handlers;

// tutorial
// export { GET, POST } from "~/lib/auth";
// // export const runtime = "edge"; // optional | prisma doesn't work on the edge by default
